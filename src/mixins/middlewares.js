import wepy from "wepy";

export default class Transform extends wepy.mixin {

    methods = {

    }
    /*
    *   手机号加密
    * */
    // transformPhone( phone ){
    //     let start = phone.substring(0,3);
    //     let end = phone.substring( 7 );
    //     return start + "****" + end;
    // }
}
